import { useEffect } from "react";
import { useRouter } from "next/router";


export default function CreatePost({ isLoggedIn, loginUser }) {

    return(
        <>
            <main>
                <p>Hi</p>
            </main>
        </>
    );
}